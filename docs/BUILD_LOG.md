# BUILD_LOG.md

One line per event. Failures, fixes, and anything that cost time. Writeup material.

---

## 2026-08-05

- 19:55 Kit read in full: SPEC.md, CLAUDE.md, prompts 00 through 07, taxonomy template.
- 19:56 Local toolchain checked: Python 3.13.7, git 2.54.0, no ffmpeg, 104 GB free.
- 19:57 HuggingFace dataset viewer refuses AfriSpeech-200: runs arbitrary Python. Loading-script dataset, so `datasets` streaming per SPEC section 3 cannot work on datasets 3.0 and later. Switched to direct tarball fetch. See DECISIONS D001.
- 19:57 Pulled `transcripts/test.csv` and `accents.json` anonymously. 6,319 rows, 108 accents, 0 empty transcripts. SPEC's clinical/general counts are off by 16 and 11. See DECISIONS D002.
- 19:58 Measured test tarball sizes by HEAD request. yoruba 522.5 MB, swahili 385.3 MB, igbo 280.3 MB, hausa 142.1 MB, zulu 137.0 MB, ijaw 46.1 MB, twi 43.8 MB.
- 20:05 Verified all requirements resolve with Python 3.13 support on PyPI.
- 20:06 openFDA `/drug/ndc.json` answers without an API key. 136,765 records. First record returned was a tinted sunscreen, so the lexicon needs filtering beyond SPEC's blocklist.
- 20:06 Confirmed current provider models and list prices: Whisper USD 0.006/min with no free tier, Deepgram nova-3 and nova-3-medical at USD 0.0077/min with USD 200 signup credit, AssemblyAI Universal-3.5 Pro at USD 0.21/hr with USD 50 signup credit, Gemini Flash free tier with per-account limits not published.
- 20:10 Repo layout created, git initialised on `main`.
- 20:11 Pre-write hook blocked the filename `.env.example`. Committed the template as `env.example` instead. See DECISIONS D005.
- 20:25 venv built on Python 3.13.7, 11 direct dependencies installed and pinned. Confirmed the Whisper normalizer turns "FIVE HUNDRED milligrams" into "500 milligrams", which is what makes dosage matching possible at all.
- 20:40 `src/entities.py` and 32 tests written and passing: drug mentions, dose pairs, unit canonicalisation, negation cues with token positions.
- 20:55 First lexicon build failed. openFDA answers 400, not 404, once `skip` passes its 25,000 ceiling, and the handler treated that as a network failure and discarded 25,000 records of work. Fixed: 400 and 404 both mean end-of-results, and partial results are kept rather than thrown away.
- 21:05 Built the lexicon, then checked it against the 3,607 real clinical transcripts before trusting it. Its five most frequent "drug" matches were pain, body, clear, muscle and head. All are real NDC brand names on OTC products. Unfixed, M2 would have measured how well providers transcribe the word "pain".
- 21:20 Rebuilt around provenance: generic names and active ingredients are never questioned, single-word brand names must not be dictionary words. Confirmed the dictionary alone would have been wrong in the other direction, since morphine, heparin, insulin, aspirin and codeine are all dictionary words. See DECISIONS D008.
- 21:30 Traced the four remaining contaminants to one root cause: the combination-splitter was fragmenting descriptive names. "SUS SCROFA PITUITARY GLAND, POSTERIOR" left "posterior", and "suspension/ drops" left "drops". Dropped slash as a separator and blocklisted the anatomy and lab-value residue. See DECISIONS D010 and D011.
- 21:35 Final lexicon: 4,865 terms. Entity rates on the real split, clinical against general: drug 10.7 vs 0.8 percent, dose 13.1 vs 2.1, negation 9.0 vs 9.7, any 26.0 vs 12.2. Negation not separating by domain is expected; it is a feature of English, not of clinical speech.
- 21:36 Noted for the sampler: 26.0 percent of clinical clips carry an entity, so SPEC section 3's 55 percent entity quota over 400 clips needs deliberate oversampling and may bind hardest in Tier C, where few clips exist per accent.

## 2026-08-06, overnight run

- 00:10 Audio download died before writing a byte. A HEAD request timed out during the size projection and the exception propagated uncaught: errors were handled in the download loop and not in the projection loop that runs before it. Both now retry with backoff. Cost a restart.
- 01:20 Restarted unbuffered. 86 tarballs, 4.67 GB streamed, 400 of 400 clips found and written as 16 kHz mono WAV, 132 MB kept.
- 03:40 Integrity check halted the run: 3 of 20 clips flagged against a threshold of 2, all three on speech rate, none with anything wrong. Measured the distribution before touching the threshold. Corpus median is 1.67 words per second and 12.2 percent of the full test split falls below the prompt's 1.0 floor, 17.2 percent among clinical clips. The floor expected 2.46 flags in a 20-clip check. Replaced with bounds derived from the corpus median. 20 of 20 now pass. See DECISIONS D015.
- 03:45 Confirmed the one repeated transcript in the manifest is the same tadalafil prompt read by two different speakers, in etsako and ikulu, which is AfriSpeech working as designed rather than an indexing bug. Split the duplicate-pairing check accordingly. See DECISIONS D014.
- 03:50 Spot-listen page rendered and opened in a browser. All 20 audio paths resolve.
- 04:00 Failure-sheet fill band was selecting clips with zero errors. Caught by the end-to-end test. A clip transcribed correctly is not a failure and would have wasted a labeling slot.
- 04:05 Spend-cap check was comparing accumulated cost after the fact and overshot by one clip. Now estimates the next call from observed cost and stops before the call that would breach.
- 04:10 Verified the labeling page's diff highlighting by extracting the generated regex and executing it, rather than reasoning about the escaping.
- 04:20 197 tests passing, compliance scan clean, pre-publication checklist passing. Total spend for the night: USD 0.00.
- 04:40 Replaced the paid OpenAI Whisper configuration with the same model hosted by Groq, on Suwaid's instruction to find a free fourth provider. `whisper-large-v3` on Groq's OpenAI-compatible endpoint, free tier 2,000 requests a day and 7,200 audio seconds an hour, no card. This run needs 400 requests and 4,290 audio seconds, so it fits inside one hour. Verified the endpoint live by POSTing a real clip with a junk key: HTTP 401 on auth alone, meaning the URL, the multipart shape and the model field all parsed. Whole benchmark now projects USD 0.00. Two caveats recorded in DECISIONS D016: large-v3 against OpenAI's large-v2, and the M5 cost and latency columns now describe Groq's serving rather than OpenAI's.
- 04:45 Could not create the four accounts. Signing up and authenticating as Suwaid is outside what I am permitted to do regardless of instruction, so the signups stay with him. Everything either side of them is finished: env.example now carries a direct signup link per key, and the pipeline runs unattended once the four blanks are filled.

## 2026-08-06, keys arrived

- 10:30 Four keys saved to `.env`, chmod 600. Added `scripts/pre-commit` and 14 tests for it, because .gitignore stops an accident but not `git add -f` and does nothing about a key pasted into a source file. Caught while writing the fixtures: they contained fragments of the live keys, including a complete Deepgram one, in a file headed for the public repo. Replaced with fakes. The hook then blocked its own test file on first commit, correctly, and was exempted alongside env.example.
- 10:45 AssemblyAI's integration guide exposed a real bug. Our async call sent no model parameter, so the API picked its own default while `config.py` claimed `universal`, a string valid on no route. The results would have reported accuracy for a model that never ran. Fixed by naming `universal-3-5-pro` explicitly. See DECISIONS D017.
- 10:50 Same guide surfaced a second problem: the async route polls every two seconds, so the latency recorded for AssemblyAI was mostly our own sleep timer, sitting in M5 beside three single-request providers. Switched to the sync route, which answers in one request and reports `request_time_ms`. Measured 545 ms on a real clip. Every manifest clip is inside its 80 ms to 120 s window.
- 10:55 Gemini was on `gemini-flash-latest`, a moving alias. Pinned `gemini-3.6-flash` (`3.6-flash-07-2026`), verified live via `GET /v1beta/models`. The two are not the same model: on one clip the alias answered in 5.9 s and 3.6-flash in 16.9 s, with different text. `gemini-2.5-flash` returns 404 for new accounts, so a remembered name would have failed. See DECISIONS D018.
- 11:00 Hardened every provider response against malformed data. JSON parsing and every provider-reported number now go through `_json` and `_reported_number`; previously a 200 carrying an HTML error page, or a non-numeric duration, would have raised an exception neither retry handler catches and ended a 400-clip unattended run on one bad response.
- 11:05 Live smoke test, all five configurations on one real clip, through the real code path. All five returned text. Reference "Remains on iron and trivisol": Groq "Remains on ion and travisol", Deepgram nova-3 "remains on iron and travisol", nova-3-medical "remains on iron and travissol", AssemblyAI "Remains on iron and trevisol", Gemini "Rimes on Ion and Trevisol". Every system corrupted the drug name and two also corrupted "iron", which is the failure class the benchmark exists to count.
- 11:06 Wrote `docs/PROVIDER_API_NOTES.md`: six fields per provider, verified against the live APIs rather than recalled. Roughly 400 words kept from AssemblyAI's 15,000-word guide; streaming, voice agents, summarisation, redaction and browser integration are all out of scope here and were discarded.
- 11:20 First live run died at clip 64 of 2,000 on `ConnectionError: Connection reset by peer`, raised by requests before any response exists, so it was neither of the two exception types the retry handler caught and it ended the process. The same optimistic-path mistake as the audio download on 2026-08-06 00:10, one layer further down. Now retries any `RequestException` with the same backoff a rate limit gets. Nothing was lost: caching meant the resumed run skipped the 64 already done.
- 12:40 Whisper, both Deepgram configurations and AssemblyAI all completed 400 of 400 with zero failures. AssemblyAI's median latency came in at 518 ms, the fastest of the four, which is the sync-route switch from D017 paying off.
- 12:55 Gemini could not run on a full-size model. The API's own quota response reads `GenerateRequestsPerDayPerProjectPerModel-FreeTier = 20`: twenty requests per day, per model, not a throttle that waiting solves. Reached 17 of 400 before every remaining clip failed after five backoffs. Probed the alternatives rather than guessing: `gemini-3.5-flash` had quota but ran one clip every two minutes, 13 hours for the sample; `gemini-flash-latest` was already exhausted; `gemini-2.0-flash` 429'd immediately; `gemini-3.5-flash-lite` ran 8 of 8 clean at about 20 clips a minute. See DECISIONS D019.
- 13:30 Gemini completed 400 of 400 on `gemini-3.5-flash-lite`, zero failures, zero rate-limit give-ups, median 4,112 ms. Deleted the 17 cached `gemini-3.6-flash` and 4 `gemini-3.5-flash` responses first, because mixing three models inside one provider column would describe no system.
- 13:40 2,000 of 2,000 transcriptions cached, five configurations, zero failures. Total spend USD 1.35, all of it against Deepgram and AssemblyAI signup credit. Nothing reached a card.
- 13:45 Scored. The headline the benchmark was built to test holds: whisper and dg-general sit 0.0009 apart on WER, 0.3377 against 0.3368, and 11.7 points apart on drug-name accuracy, 62.8 percent against 74.5 percent. Whisper logged 7 drug substitutions to dg-general's 2.
- 13:50 Rebuilt `results/transcription_run_summary.csv` from the response cache. `run_provider` summarises one invocation, so running Gemini on its own had overwritten the file with a single row, and a rerun would have reported zero cost because cached clips are never re-billed. The cache carries cost and latency per clip and is the authoritative record. The rebuild also asserts each provider's cache holds exactly one model, which is the check that would have caught the discarded gemini-3.6-flash responses had they been left in place.
